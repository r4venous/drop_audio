# samples/

Place raw `.wav` recordings here.

```
samples/
├── raw/          ← unprocessed field recordings
└── processed/    ← filtered / normalised outputs
```

Files are excluded from version control via `.gitignore`.
